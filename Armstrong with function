#include<iostream.h>
#include<conio.h>
void armie(int n)
{
 int d,temp,sum=0;
 temp=n;
 while(n>0)
 {
  d=n%10;
  sum+=d*d*d;
  n=n/10;
 }
 if(sum==temp)
 cout<<"Armstrong";
 else
 cout<<"NO";
 }
 void main()
 {
  clrscr();
  int a;
  cout<<"Enter a number";
  cin>>a;
  armie(a);
 }
